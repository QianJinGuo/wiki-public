---

title: "解构Scaling Law：优化、架构、数据的三重奏"
created: 2026-08-30
updated: 2026-08-30
type: entity
tags: ['harness', 'training', 'ai']
sources: [raw/articles/解构scaling-law优化架构数据的三重奏]
confidence: 0.7
---

# 解构Scaling Law：优化、架构、数据的三重奏

# 解构Scaling Law：优化、架构、数据的三重奏

苏剑林 2026-08-24 18:38 北京

Scaling Law还能这样统一

©作者 | 苏剑林

单位 | 科学空间

研究方向 | NLP、神经网络

训练一个大型的神经网络，最终效果会受到非常多因素的影响，换个优化器，换个模型架构，或者换一个训练集，结果都可能截然不同。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

在工程实践中，我们将调试这些因素的经验结果，戏称为“炼丹”。但如何从经验上升到规律，更准确、定量地描述它们之间的关系呢？ ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

如果能搞清楚这个问题，我们的炼丹将会更有底气。

“Scaling Law” 便试图以一种相对定量的方式来回答这个问题。

自 OpenAI 在 2020 年的奠基性工作（Kaplan Law [1]）以来，Scaling Law 已成为深度学习最可靠的经验规律之一，它可以用来预估模型性能、指导超参数选择、判断某个改动的有效性等。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

为此，也有不少工作，试图对 Scaling Law 做进一步的更贴近本质的解读。

本文也来分享笔者对 Scaling Law 的一些理解。

准备篇

后文会反复遇到“在某个约束下，求幂律组合的最小值”这类问题，所以我们这一篇先准备两个基础结论。

1.1 异幂不等式

首先是一个使用次数最多的不等式，设 ，那么：

取等号的条件是：

它是简单不等式  的推广，可以通过求导或者加权 AM-GM 不等式 [2] 来证明。

下面演示后者，加权 AM-GM 不等式为 ，于是我们有：

等号成立的条件为 ，即 。为了方便使用，我们称这个不等式结论为“异幂不等式”，它的主要特点是有两项指数异号的幂函数。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

更有趣的是，最小值点和最小值本身依然还是幂律，这是后续一系列推导的基石之一。

1.2 最优配比率

异幂不等式是固定乘积（）后，求两项幂律之和的最小值，还有一些场景（比如求参数量的最优配比）需要我们要固定它们的“和”来求最小值。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

具体来说，设 ，我们要求：

很遗憾，这个问题没有初等的解析解，但数值求解是没有问题的。不难看出最小值一定是存在的，将  代入，然后求导得 ，令它等于零得： ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

左端在  内关于  显然是单调递增的，并且  时，左端趋于 ，当  时，左端趋于 0。由介值定理可知它在  内存在唯一解，直接二分法即可求得。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

思想篇

一个模型的训练过程，可以形式化为：

在给定数据 、架构  和优化器  之下，最小化损失函数 。

这里  表示某个理想分布，也可以将它想象为一个无比巨大的测试集，而我们能构建的任意训练集 ，都是它的一个子集，或者说一个采样结果； 则表示在这些条件之下，模型在理想分布  上能达到的损失值。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

2.1 三重分解

我们考虑如下分解：

这个分解看上去将问题复杂化了，但实际上它将当前训练状态到理想目标的距离，分解成了层层递进的三步。

在“越多越好”的常规假设下，每个括号都是非负的，因此它相当于把总差距写成三段各自可解释的距离之和，尽可能解耦了各个变量对损失函数的影响，使我们可以更合理地推测它们之间的依赖关系。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

现在我们就来逐一解释式中每一项的含义。

2.2 数据误差

第一层分解是：

其中  代表着给定架构  和优化器  下，模型在训练集  上的损失值。

注意，根据定义， 代表模型在理想分布  下的损失，这是我们的终极目标。

然而， 在训练过程中是不可触碰的，我们只能跟训练集  打交道，所以只能得到训练损失 ，然后设法通过  来描述它们之间的差距。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

这一项通常也叫“泛化误差”，其关键影响因素是数据——比如数据的数量、质量、多样性，等等。

此外，架构 、优化器  也可能改变泛化误差，具体要考虑哪些变量，取决于我们的分析目标。

2.3 优化误差

第二层分解是：

其中  代表把优化推到极致——比如有一个完美的超级优化器，或者把训练步数与调参次数都推向无穷——时，训练集上能达到的理想损失。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

所以， 代表了优化器的天花板，而  则表示实践优化器与这个天花板的距离，它衡量的是优化器是否足够好——比如学习率是否合适、训练步数够不够、批大小是否足够稳定梯度，等等。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

2.4 架构误差

第三层分解是：

其中  代表优化和架构都推到极致——任意优秀的优化器、任意强大的模型——时，训练集上能达到的最理想损失。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

所以， 是当前这份数据自身决定的理论极限，而  则表示实践模型与其理论极限的效果差距。

它衡量的是架构是否足够好——参数量够不够、深度宽度够不够、残差还有没有改进空间，等等。

优化篇

这一节我们先来探讨优化差距 。给定优化器（比如 Adam 或 Muon）后，我们主要关心学习率 、批大小 B 和训练步数 T 这三个核心参数的影响。 ^[raw/articles/解构scaling-law优化架构数据的三重奏.md]

当然，理论上可以把其他参数如动量还有其他细节都

→ [[raw/articles/解构scaling-law优化架构数据的三重奏|原文存档]]
